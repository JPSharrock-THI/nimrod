package com.nimrod.csv;

import org.springframework.stereotype.Component;

/**
 * Reads a CSV file and extracts rows, identifying columns that contain
 * FlatBuffer-serialised binary data.
 */
@Component
public class CsvReader {
    // TODO: Phase 2 — CSV ingestion with Apache Commons CSV
}
